spring:
  application:
    name: adventures
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true
  h2:
    console:
      enabled: true
      path: /h2-console
  sql:
    init:
      mode: never

server:
  servlet:
    context-path: /
  error:
    include-message: always
    include-binding-errors: always
    include-stacktrace: on_param
    include-exception: false

logging:
  level:
    root: INFO
    com.ynov.adventures: DEBUG

app:
  jwt:
    # Clé HMAC-SHA256 encodée en Base64 — générer avec : openssl rand -base64 32
    # En prod : variable d'env / vault / secret manager
    secret: dSrE+nKozsf8FrKoAWsPiWRILLb3WWXLBYRqkZ8u7G8=
    issuer: adventures-api
    exp-minutes: 60

